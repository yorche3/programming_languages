# Hello User

create a file named `hello_user.ext`/`HelloUser.ext` that ask for user name and prints "Hello, <name>! from".

```pseudocode
ask "What is your name?" and store answer in name
show "Hello, " + name + "!"
```