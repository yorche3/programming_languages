import numbers_iterative

fn test_fibonacci() {
    assert numbers_iterative.fibonacci(5) == 5
}

fn test_factorial() {
    assert numbers_iterative.factorial(5) == 120
}

fn test_sum_numbers() {
    assert numbers_iterative.sum_numbers(5) == 15
}