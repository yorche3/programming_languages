[indent=2]

/*
Función que regresa un saludo
*/
def hola():string
  return "Hola";

def repeticiones(word:string, ch:char)
  var l = word.length;
  count:int  = 0;
  for var i = 0 to l
    if word[i] == ch
      count++;
  print "%d", count;

init
  print hola();
  repeticiones("hello", 'e');